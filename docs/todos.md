### Post Release Updates
- cookie should update on every request so that we don't get logged out while using
- create api command to generate ACTIVE test user
- flac format input
- update help link
- listener must get turned on by hand :( when server starts.
- does each user get a table with a log?
- when "already subscribed", go to a different page
- clean up log
- storage!
    - cron rsync log files out
    - cron delete log files
    - keep record of all jobs in DB (or CSV and then rsync out)
    - upload completed files to S3 with [boto3](https://boto3.amazonaws.com/v1/documentation/api/latest/index.html)
    - cron removal of uploaded tracks
    - cron removal of separated tracks
- add http redirect
- listener.sh
    - more info on the listener log
- assert listener is running
- listener has to use demucs command from env!
    -   open `screen`, activate env
- Security
    - secure and test against SQL injection
- email should go to signed in user? - maybe
- create new email address to be the demucs sender all this
- Figure out cost spread w Sukhi
